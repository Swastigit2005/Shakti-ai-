<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_insight"
    android:layout_width="200dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeWidth="2dp"
    app:strokeColor="#FF6B9D"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Icon (optional) -->
        <ImageView
            android:id="@+id/img_insight_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="12dp"
            android:src="@android:drawable/ic_menu_add"
            android:visibility="gone"
            tools:visibility="visible"
            android:contentDescription="Insight Icon" />

        <!-- Title -->
        <TextView
            android:id="@+id/tv_insight_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Log your symptoms"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginBottom="8dp"
            android:gravity="center" />

        <!-- Description -->
        <TextView
            android:id="@+id/tv_insight_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Track how you're feeling today"
            android:textSize="14sp"
            android:textColor="#424242"
            android:layout_marginBottom="12dp"
            android:gravity="center" />

        <!-- Action Text -->
        <TextView
            android:id="@+id/tv_insight_action"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add now"
            android:textSize="14sp"
            android:textColor="#00ACC1"
            android:textStyle="bold"
            android:layout_gravity="center"
            android:visibility="gone"
            tools:visibility="visible" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
